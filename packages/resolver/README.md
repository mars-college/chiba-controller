# cable3 resolver

Target resolution engine for cable3.

Inputs:
- semantic target (`media|playlist|block|channel|profile`)
- display/options policy

Outputs:
- `RenderManifestV1`
- dependency set and cache requirements
